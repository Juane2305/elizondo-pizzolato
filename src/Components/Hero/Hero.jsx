import React from 'react';
import { ImageSlider } from '../ImageSlider/ImageSlider';

const Hero = () => {
    return (
        <div>
            <ImageSlider/>
        </div>
    );
};

export default Hero;